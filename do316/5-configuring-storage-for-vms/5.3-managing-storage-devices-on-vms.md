
Add several new storage devices to a virtual machine, migrate data from an existing storage device to the new storage devices, reconfigure the virtual machine and its applications to use the new storage devices, and then remove the old storage device with the data.

1- Create a database VM 

Create a VM named db-server

2- Attach an NFS slow storage

Attach a SATA disk, which non hotplug disk. Hotplug in only enabled for SCSI disks.

3- Install mysql-server 

Install mysql-server on the db-server VM

4- Create a database named prod


5- Attach three additional hotplug disks

Attach three additional hotplug SCSI disks 

6- Migrate the database

Migrate the data, redo and undo to the three disks